language: ruby

sudo: required

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -y libvirt-dev
 - gem update bundler

script: bundle exec rake

matrix:
  fast_finish: true
  include:
    - rvm: 2.0.0
    - rvm: 2.1.0
    - rvm: 2.1.1
    - rvm: 2.1.1
      env: FOG_LIBVIRT_DEPS_GIT=1
    - rvm: 2.2.0
    - rvm: 2.2.0
      env: FOG_LIBVIRT_DEPS_GIT=1

notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#ruby-fog"
    template:
      - "[#%{build_number}] %{message} %{build_url}"
      - "[#%{build_number}] %{commit} on %{branch} by %{author}"
      - "[#%{build_number}] %{compare_url}"
    on_success: always
    on_failure: always
    use_notice: false
